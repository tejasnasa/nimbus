{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "env": ["DATABASE_URL", "NEXT_PUBLIC_BACKEND_URL"],
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^build", "^check-types"]
    },
    "dev": {
      "dependsOn": ["^db:generate"],
      "cache": false,
      "persistent": true
    },
    "db:generate": {
      "cache": false,
      "passThroughEnv": ["DATABASE_URL", "NEXT_PUBLIC_BACKEND_URL"]
    },
    "db:migrate": {
      "cache": false,
      "passThroughEnv": ["DATABASE_URL", "NEXT_PUBLIC_BACKEND_URL"]
    },
    "db:deploy": {
      "cache": false
    },
    "test": {
      "dependsOn": ["^db:generate"],
      "cache": false,
      "passThroughEnv": ["DATABASE_URL", "REDIS_URL", "NODE_ENV"]
    },
    "test:coverage": {
      "dependsOn": ["^db:generate"],
      "cache": false,
      "outputs": ["coverage/**"],
      "passThroughEnv": ["DATABASE_URL", "REDIS_URL", "NODE_ENV"]
    }
  }
}
